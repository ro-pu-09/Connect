import 'package:flutter/material.dart';


class cartcust extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text("Cart"),
          actions: [
            FlatButton(onPressed: null, child: Text("buy")),
          ],
      ),
      body: ,

    );
  }
}
